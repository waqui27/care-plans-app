# dependencies
node_modules/

# environment — holds JWT secret + seeded credentials, never commit
# ignores .env, .env.local, .env.production, etc. in any folder; keeps .env.example
.env
.env.*
!.env.example

# local MongoDB data
.mongo-data/

# builds
client/dist/
dist/

# logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
