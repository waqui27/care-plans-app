# copy to client/.env (or .env.production) when the API is on a different domain.
# Full base URL of the API, including the /api path. Leave unset for same-origin
# / local dev (the Vite proxy in vite.config.js forwards /api to localhost:4000).
VITE_API_URL=https://your-api-host.example.com/api
